"""Uma academia deseja fazer um senso entre seus clientes para descobrir o mais alto,
o mais baixo, a mais gordo e o mais magro, para isto você deve fazer um programa que
pergunte a cada um dos clientes da academia seu código, sua altura e seu peso.
O final da digita(zeroção de dados deve ser dada quando o usuário digitar 0 ) no campo
código. Ao encerrar o programa também deve ser informados os
códigos e valores do clente mais alto, do mais baixo, do mais gordo e do mais magro,
além da média das alturas e dos pesos dos clientes"""



codigo = int(input('digite seu código'))
altura = float(input('Sua altura'))
peso = float(input('Seu peso'))
lista_codigo = []
lista_altura = []
lista_peso = []
while codigo > 0:
    codigo = int(input('digite seu código'))
    altura = float(input('Sua altura'))
    peso = float(input('Seu peso'))
else:
    for i,a in enumerate(lista_altura):
        if a (max(lista_altura)):
            lista_altura.append(a)
            print(lista_altura)


        

    
    
